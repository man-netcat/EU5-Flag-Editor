PySide6>=6.5
Pillow>=9.0

# DDS support (game assets are .dds). Install one of these manually if needed:
# Option A: pillow-dds (preferred if available for your platform)
#   pip install pillow-dds
# Option B: imageio with FreeImage backend
#   pip install "imageio[freeimage]"
